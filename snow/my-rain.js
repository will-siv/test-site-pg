setTimeout(() => {
    // 13-51 removed for not wanting snow
    // isSlow? idk what that does
    // not using thunder for simplicity
    let power = Math.sin(Date.now() / 90000000); // power of droplets?
}
, 1000);
